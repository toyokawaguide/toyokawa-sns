name: SNSトークン健康診断（毎日・再発防止）

# 2026-06-16 新設。Threadsトークンが6/14に無監視のまま期限切れ→数日Threads投稿停止した件の再発防止。
# 毎日 META と Threads の両トークンを点検し、問題がある時だけ Gmail で警告（健全なら無音）。

on:
  schedule:
    - cron: '0 0 * * *'   # 毎日 09:00 JST（UTC 00:00）
  workflow_dispatch:

permissions:
  contents: read

jobs:
  health:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    defaults:
      run:
        working-directory: uranai/scripts
    steps:
      - uses: actions/checkout@v4
      - name: Python セットアップ
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: ライブラリインストール
        run: pip install requests google-api-python-client google-auth google-auth-oauthlib
      - name: トークン健康診断
        env:
          META_ACCESS_TOKEN: ${{ secrets.META_ACCESS_TOKEN }}
          THREADS_ACCESS_TOKEN: ${{ secrets.THREADS_ACCESS_TOKEN }}
          THREADS_USER_ID: ${{ secrets.THREADS_USER_ID }}
          GMAIL_USER: ${{ secrets.GMAIL_USER }}
          GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
          # Google OAuth トークン点検（2026-06-18 追加・ライト記事/記事SNSの失効を毎日検知）。
          # SNS_GOOGLE_TOKEN_B64 は toyokawa-sns 未登録なら空文字→自動skip（記事SNS分は社長がSecret追加で有効化）。
          LIGHT_GOOGLE_TOKEN_B64: ${{ secrets.LIGHT_GOOGLE_TOKEN_B64 }}
          SNS_GOOGLE_TOKEN_B64: ${{ secrets.SNS_GOOGLE_TOKEN_B64 }}
        run: python token_health_check.py
